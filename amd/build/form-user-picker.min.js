define("tool_formbuilder/form-user-picker",["core/ajax","jquery"],(function(ajax,$){return{transport:function(selector,query,success,failure){let promises=null;void 0===query&&(query="");let calls=[{methodname:"tool_formbuilder_search_users",args:{search:query}}];promises=ajax.call(calls),$.when.apply($.when,promises).done((function(data){success(data)})).fail(failure)},processResults:function(selector,data){let results=[];for(let i=0;i<data.users.length;i++){let label=data.users[i].fullname;data.users[i].idnumber.length&&(label+=" ("+data.users[i].idnumber+")"),results.push({value:data.users[i].id,label:label})}return results}}}));

//# sourceMappingURL=form-user-picker.min.js.map